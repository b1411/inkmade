<script setup lang="ts">
// INK SYSTEM · координаты (спека §36.2). Базовый мотив бренда — привязка к Алматы.
//
// Дозировка (§36.3): не более ОДНОГО набора координат на экран. Сейчас их два места —
// hero и футер, они физически не попадают в один viewport. Третьего быть не должно:
// «не использовать координаты в каждой секции».
//
// Литерал, а не i18n: это фирменная метка, как EST. 2025 на логотипе, — она не
// переводится. Градусы и полушария читаются одинаково в ru и kk.
withDefaults(defineProps<{ city?: boolean }>(), { city: true })
</script>

<template>
  <p class="ink-label text-ink-bone/58">
    43.2389° N, 76.8897° E<span v-if="city"> · ALMATY, KAZAKHSTAN</span>
  </p>
</template>

