/**
 * INK SYSTEM — фирменный графический код INKMADE (спека §36).
 *
 * Здесь живут только те значения, которые нужны коду, рисующему в <canvas>:
 * Konva не видит CSS-переменных, ему нужны готовые строки. Всё остальное берётся
 * из токенов app/assets/css/main.css — дублировать палитру в JS не надо.
 *
 * ВАЖНО: эти хексы обязаны совпадать с §3.1. Правишь main.css — правь и здесь,
 * иначе холст снова разъедется с сайтом, как было с `#7A1F28` (старый бордо,
 * переживший миграцию палитры) и `#E2B23A` (золотой, которого в каноне нет вовсе).
 */
export const INK_CANVAS = {
  /** INKMADE Burgundy (§3.1). Акцент и дефолтная заливка новых фигур/текста. */
  burgundy: '#7E1F2D',
  /** Bone — рамка зоны печати на тёмном (§36.2). */
  frameOnDark: 'rgba(243, 240, 235, 0.55)',
  /** Ink Black 40% — та же рамка на светлом изделии (§36.2). */
  frameOnLight: 'rgba(8, 11, 13, 0.40)',
  /** Selected state рамки — бордо (§36.2). */
  frameSelected: '#7E1F2D',
  /** Смарт-направляющие. Muted Warning из канона (§3.1) вместо самодельного золота. */
  guide: '#C89A4B',
} as const

/** Длина угловой метки, px (§36.2 допускает 8–12). */
export const CROP_MARK_LEN = 10
/** Зазор между рамкой и меткой, px — как в настоящей допечатной подготовке. */
export const CROP_MARK_GAP = 5

