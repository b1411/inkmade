# Что требует вас лично

Живой список действий, которые я не могу выполнить за вас. Обновляется по мере работы.
Контекст — [AUDIT_2026-07-20.md](AUDIT_2026-07-20.md) и [PLAN_TO_100_2026-07-20.md](PLAN_TO_100_2026-07-20.md).

---

## 🔴 Срочное — безопасность

### 1. Проверить публичность репозитория
`github.com/b1411/inkmade` — открыть и посмотреть бейдж Public/Private.
Я проверить не смог: доступ к GitHub заблокирован классификатором сессии.

**От ответа зависит масштаб:**
- **Private** → риск ограничен теми, у кого есть доступ к репо. Действия ниже всё равно нужны.
- **Public** → это инцидент. Дополнительно: ротировать **все** ключи, когда-либо попадавшие
  в репозиторий, и просмотреть логи доступа Supabase на предмет чужих сессий.

### 2. Сменить скомпрометированные пароли
Пароль `Inkmade2026!` лежит в `scripts/seed-accounts.mjs:13`, продублирован в
`SESSION_HANDOFF_2026-07-18.md:144` и **находится в истории git** (коммиты `285082c`, `177c7ea`)
— смены файла недостаточно, пароль скомпрометирован навсегда.

- [ ] Сменить пароли: `admin@`, `manager@`, `designer@`, `customer@`, `company@` inkmade.kz
- [ ] Удалить/деактивировать в проде лишние тестовые аккаунты
- [ ] Ротировать `SUPABASE_SERVICE_ROLE_KEY`
- [ ] Ротировать `PAYMENT_WEBHOOK_SECRET`
- [ ] Включить MFA на админ-аккаунте (Supabase Auth)

### 3. Настройки Supabase Auth (только через дашборд)
- [ ] Проверить, что **Confirm email** включён — иначе регистрация на чужой адрес даёт рабочий аккаунт
- [ ] Минимальная длина пароля → 12, включить проверку по HIBP
- [ ] Перевести Auth с абсолютных 10 коннектов на percentage-based (advisor `auth_db_connections_absolute`)

---

## 🔴 Применение миграций в прод

Прод на `0090`. Я подготовил файлы, но **применять не буду без вашей команды.**

| Миграция | Что делает | Риск |
|---|---|---|
| `0091_shop_payouts.sql` | Уже в репо, не применена. Создаёт `shop_payouts` + RPC выплат | Низкий — новые объекты |
| `0092_studio_queue_stage_sla.sql` | Уже в репо, не применена. Добавляет `status_changed_at` в очередь студии | Низкий |
| `0093_shop_earnings_reversed_status.sql` | **Новая, мной.** Чинит блокер: возврат B2B-заказа падает на CHECK | Низкий — таблица пуста |
| `0094_money_integrity_constraints.sql` | **Новая, мной.** Денежные инварианты, индексы, триггеры, FK | Средний — см. ниже |

**Порядок:** 0091 → 0092 → 0093 → 0094.

⚠️ **0093 и 0094 не проверены против БД** — доступ к Supabase MCP в моей сессии заблокирован.
Я сверил все имена таблиц, колонок и сигнатур по файлам миграций, но выполнить их не мог.
Прогоните сначала на preview-ветке Supabase либо будьте готовы откатить.

Отдельно в 0094 намеренно **нет** CHECK на `royalty_earnings.amount`/`shop_earnings.amount`:
миграция 0022 документирует отрицательные суммы как штатное сторнирование.

### После применения
- [ ] Перегенерировать `app/types/database.types.ts` из прода — сейчас типы описывают
      `shop_payouts` и `status_changed_at`, которых в проде нет (typecheck зелёный, ломается рантайм)
- [ ] Выгрузить в файлы 3 прод-объекта без локальной миграции, сохранив version-таймстемпы:
      `studio_rpc_revoke_anon`, `0079b_revoke_public_admin_rpc`, `print_zone_canvas_bounds_check_fix`.
      Без этого `supabase db reset` даёт схему с другими грантами и другим CHECK.

---

## 🟠 Настройки платформ

### GitHub
- [ ] Включить **branch protection** на `main`, required check = job `quality`.
      Без этого починенный CI всё равно не помешает влить сломанный код.
- [ ] Установить repo variable `RUN_E2E = true`, когда будут seed-данные и креды
      (сейчас e2e и rls-джобы просто пропускаются)

### Vercel
- [ ] Установить Node версии **22** (я закрепил `.nvmrc` и `engines.node >= 22`)
- [ ] Проверить, что `regions: ["fra1"]` из `vercel.json` принимается вашим планом
      (Hobby разрешает один регион; Supabase проекта — в `eu-central-1`)
- [ ] Задать `ERROR_WEBHOOK_URL` (Slack/Telegram) либо подключить Sentry —
      сейчас единственный канал ошибок это `console.error` в логах Vercel
- [ ] Внешний uptime-монитор на `/health?deep=1` (UptimeRobot / BetterStack)

### Локально
- [ ] Проверить `pnpm --version`. Я поставил в CI `version: 9` (lockfileVersion 9.0).
      Если у вас 10.x — либо приведите к 9, либо поправьте `.github/workflows/ci.yml`
      и добавьте `packageManager` в `package.json`. Я эту команду выполнить не смог.

---

## 🟠 Ручная работа, которую нельзя сделать кодом

### Калибровка печатных зон ⭐ самый заметный дефект для клиента
**11 из 16 прод-зон имеют `bounds_canvas = NULL`** — превью показывает принт не там
и втрое крупнее, включая флагманскую футболку.

- [ ] Откалибровать через `/admin` → мастер товара → шаг «Зоны»:
      `tshirt/chest`, `tshirt/back`, оба рукава, `oversize-tee/chest`,
      `polo/*`, `sweatshirt/*`, `cap/cap_front` и остальные
- [ ] Проверить глазами на 2–3 товарах, что превью совпадает с реальным размером принта

Ускорить кодом нельзя — нужна визуальная сверка. Гард, запрещающий публикацию
некалиброванного товара, я добавлю в код отдельно.

### Себестоимость заготовок
- [ ] Заполнить `blank_cost` у вариантов. Сейчас default 0, поэтому `cogs = 0`
      и маржа на всех дашбордах равна 100% выручки.

---

## 🟠 Внешние интеграции

### ePay (Halyk)
- [ ] Получить доступ к sandbox `test-epay-api.epayment.kz`
- [ ] Прогнать полный цикл: invoice → charge → check-status → refund

Шлюз **ни разу не исполнялся против банка** (в проде 4 mock-платежа, 0 epay).
Два подозрения из аудита, которые проявятся именно на боевом прогоне:
`epayToken()` запрашивает токен без `invoiceID/amount/currency/terminal`;
`refund()` шлёт `amount` в теле, а не query-параметром.

### Уведомления цеху
- [ ] Решить канал: email на `OPS_NOTIFY_EMAIL`, WhatsApp-группа или Telegram-бот.
      Сейчас цех узнаёт о заказе только через открытую вкладку `/studio`.

---

## ⚖️ Юридические решения

- [ ] **Фискализация.** Решено отложить. Нужно письменное юридическое заключение
      о допустимости продаж без онлайн-ККМ/ОФД в РК. Отложенное решение
      не снимает ответственности перед первой продажей.
- [ ] **Казахская локаль юр-документов.** Оферта и политика только на русском
      (623 строки). Для РК это может быть требованием, а не UX.

---

## ❓ Продуктовые решения, жду вашего ответа

### 1. Шелкография
Продаётся и тарифицируется по числу цветов (надбавка 800 ₸), но экспорт всегда даёт
плоский RGBA-PNG. Клиент может прислать фотопринт за цену 2 трафаретов — цех
физически не исполнит заказ по оплаченной цене.

- **A (рекомендую):** убрать `silkscreen` из публичного выбора до появления препресса — 0.5 дня
- **B:** цветоделение/постеризация до N цветов + проверка на границе — 4–6 дней

### 2. Удаление фона
Не работает в проде по двум независимым причинам (импорт по переменной не бандлится
Vite + CSP без `wasm-unsafe-eval` и без хоста моделей). Пользователь видит тост «не получилось».

- **A:** починить (self-hosting моделей + правки CSP) — 1–2 дня
- **B (рекомендую для запуска):** убрать кнопку из UI. Неработающая фича хуже отсутствующей.

### 3. KK-SEO
`no_prefix` без `hreflang` → у ru и kk один URL, локаль только в cookie.
Казахоязычный запрос в Google **никогда** не приведёт на сайт.

- **A:** `prefix_except_default` + `baseUrl` + `useLocaleHead` — 1–2 дня, меняет все URL
- **B:** осознанный письменный отказ от KK-SEO

### 4. Две воронки онбординга магазина
Self-serve (`/shop-new`) и админ-шлюз (`/business` → заявка → approve → claim)
работают одновременно. Админ может создать второй магазин на тот же email,
после чего `create_my_shop` откажет владельцу. Какую замораживаем?

### 5. Стоимость доставки
Сейчас не входит в `total` вообще (статичный текст в чекауте).
Включать в расчёт или зафиксировать «доставка включена в цену»?

---

## 🔧 Блокер моей работы

**Bash/PowerShell заблокирован классификатором** — с середины сессии стабильно.
Из-за этого я не могу:

- прогнать `lint` / `typecheck` / `test` / `build` и проверить свои же правки
- сделать `git commit`
- обратиться к Supabase MCP, чтобы сверить миграции с реальной схемой

Поэтому я остановился на изменениях, которые проверяются чтением, и **не трогал**
код платежей, возвратов и печатного конвейера — править денежные пути вслепую,
без возможности прогнать тесты, безответственно.

**Чтобы я продолжил:** разрешите Bash (`/permissions` → добавить правило на
`npm run *` и `git *`, либо переключить режим на acceptEdits).
